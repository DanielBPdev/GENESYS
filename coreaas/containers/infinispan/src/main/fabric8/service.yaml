apiVersion: v1
kind: Service
metadata:
  name: ${project.artifactId}
  labels:
    app: ${project.artifactId}
spec:
  type: LoadBalancer
  ports:
  - port: 11222
    targetPort: 11222
    name: hotrod
  - port: 9990
    targetPort: 9990
    name: adm
  selector:
    name: ${project.artifactId}