<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>com.asopagos.coreaas.services</groupId>
		<artifactId>validaciones</artifactId>
		<version>0.0.2-SNAPSHOT</version>
	</parent>

	<artifactId>validaciones-service</artifactId>
	<packaging>war</packaging>
	<name>Servicio Validaciones</name>
	<description>Aplicación Web que implementa los servicios backend de validación</description>

	<properties>
		<configuracion.servidor>wildfly-swarm-validaciones</configuracion.servidor>
		<fabric8-maven.version>4.4.0</fabric8-maven.version>
		<fabric8.skip>false</fabric8.skip>
		<docker.cmd>java -Xms512m -Xmx3g -XX:+UseG1GC -jar /opt/$TIPO_SERVIDOR -s/opt/conf/${configuracion.servidor}.yaml /maven/${project.build.finalName}.war -Dswarm.context.path=${project.build.finalName} -Dinfinispan.service.host=$INFINISPAN_SERVICE_HOST -DLionConfigurationImpl=com.asopagos.lion.config.LionConfigurationAsopagosImpl</docker.cmd>
		<resourceDir.path>../../../util-develop/plantillas-despliegue/src/main/resources/kubernetes-definitions-validaciones</resourceDir.path>
	</properties>

	<dependencies>
		<!-- Dependencia para la validación de los servicios aportes y afiliados. Inicialmente usado para la validación de la postulación FOVIS -->
		<dependency>
			<groupId>com.asopagos.coreaas.services</groupId>
			<artifactId>aportes-api</artifactId>
			<version>${project.version}</version>
		</dependency>

		<dependency>
			<groupId>com.asopagos.coreaas.services</groupId>
			<artifactId>validaciones-api</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>javax.enterprise.concurrent</groupId>
			<artifactId>javax.enterprise.concurrent-api</artifactId>
			<version>1.0</version>
			<scope>provided</scope>
		</dependency>
		<dependency>
      		<groupId>com.google.cloud</groupId>
      		<artifactId>google-cloud-storage</artifactId>
      		<version>1.14.0</version>
    	</dependency>
		<dependency>
      		<groupId>com.google.http-client</groupId>
      		<artifactId>google-http-client-jackson</artifactId>
      		<version>1.22.0</version>
    	</dependency>
	</dependencies>

	<build>
		<!-- Maven will append the version to the finalName (which is the name 
			given to the generated war, and hence the context root) -->
		<finalName>ValidacionesService</finalName>
		<plugins>
			<plugin>
				<groupId>org.wildfly.plugins</groupId>
				<artifactId>wildfly-maven-plugin</artifactId>
				<configuration>
					<skip>false</skip>
				</configuration>
			</plugin>
		</plugins>
	</build>
</project>
