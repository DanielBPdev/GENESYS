<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>com.asopagos.coreaas.services</groupId>
		<artifactId>listas-chequeo</artifactId>
		<version>0.0.2-SNAPSHOT</version>
	</parent>

	<artifactId>listas-chequeo-service</artifactId>
	<packaging>war</packaging>
	<name>Servicio Listas de Chequeo</name>
	<description>Aplicación Web que implementa los servicios backend de listas de chequeo</description>

	<properties>
		<fabric8.skip>false</fabric8.skip>
	</properties>

	<dependencies>
		<dependency>
			<groupId>com.asopagos.coreaas.services</groupId>
			<artifactId>listas-chequeo-api</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>com.asopagos.coreaas.services</groupId>
			<artifactId>parametros-asopagos-api</artifactId>
			<version>${project.version}</version> 
		</dependency>
		<dependency>
			<groupId>com.asopagos.coreaas.services</groupId>
			<artifactId>validaciones-api</artifactId>
			<version>${project.version}</version>
		</dependency>
		
		<!-- rutinas -->
		<dependency> 
			<groupId>com.asopagos.coreaas.rutines.listas-chequeo-rutines</groupId>
			<artifactId>guardar-lista-chequeo-rutine</artifactId>
			<version>${project.version}</version>
		</dependency>
		
	</dependencies>

	<build>
		<!-- Maven will append the version to the finalName (which is the name 
			given to the generated war, and hence the context root) -->
		<finalName>ListasChequeoService</finalName>
		<plugins>
			<plugin>
				<groupId>org.wildfly.plugins</groupId>
				<artifactId>wildfly-maven-plugin</artifactId>
				<configuration>
					<skip>false</skip>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<profiles>
	</profiles>

</project>
